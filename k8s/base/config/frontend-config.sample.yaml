apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: video-host
data:
  api-endpoint: "https://api.your-domain.com"
  enable-comments: "true"
  require-signup-code: "false"
  site-name: "Video Host"
