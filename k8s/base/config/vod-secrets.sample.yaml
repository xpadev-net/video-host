apiVersion: v1
kind: Secret
metadata:
  name: vod-secrets
  namespace: video-host
type: Opaque
data:
  # Base64 エンコードされた値を設定してください
  # echo -n 'your-value' | base64
  vod-internal-secret: ""  # VOD内部通信用シークレット（backend-secretsと同じ値）
